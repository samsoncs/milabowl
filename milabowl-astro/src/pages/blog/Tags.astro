---
export interface Props {
    tags: string[];
}

const { tags } = Astro.props;
---
<div class="flex gap-2">
{
    tags.map(t => (
        <div class="p-1 px-4 rounded-full bg-slate-300 dark:bg-slate-700">
            {t}
        </div>
    ))
}
</div>       