---
export interface Props {
  href: string;
  disableUnderline?: boolean;
}
const { href, disableUnderline } = Astro.props;
---

<a
  class:list={['text-content-primary', { underline: !disableUnderline }]}
  href={href}
>
  <slot />
</a>
