---
export interface Props {
  title: string;
  color?: string;
}

const { title, color = 'bg-black' } = Astro.props;
---

<div class:list={['rounded-full', 'text-white', 'text-center', 'px-2', color]}>
  {title}
</div>
